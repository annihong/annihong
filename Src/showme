DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo "Live long and prosper"

The plan is to devise an algorithm that displays
the data effectively and elegantly.

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

sh "$DIR/whome"

